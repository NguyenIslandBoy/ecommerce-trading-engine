[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: runs against the built warehouse (needs dbt build + export_marts)
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
