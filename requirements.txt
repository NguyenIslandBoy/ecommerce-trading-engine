# Direct dependencies only. scipy, scikit-learn and jupyter are not used by
# Layer 1 (this dbt project) -- they are pinned here for the Layer 2
# detection and Layer 3 recommendation/simulation work that follows.
dbt-core==1.12.3
dbt-duckdb==1.11.0
duckdb==1.5.5
pandas==3.0.5
numpy==2.5.2
scipy==1.18.1
scikit-learn==1.9.0
jupyter==1.1.1
pytest==8.4.2
streamlit==1.50.0
